1. Upload all project files to the public_html (or www) folder using File Manager or FTP.
 2. Create a MySQL database and database user on the hosting panel.
 3. Export the local database from phpMyAdmin as an .sql file.
 4. Import the .sql file into the server database via phpMyAdmin.
 5. Update database connection settings in the PHP config file (host, database name, user, password).
 6. Open the website in a browser and check that it works.

 ## Contributors

- @moredax — backend
- @fronxod-f — frontend
